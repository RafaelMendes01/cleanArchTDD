// eslint-disable-next-line @typescript-eslint/no-var-requires
const express = require('express')

const app = express()
app.listen(5050, () => console.log('Server running at https://localhost:5050'))
